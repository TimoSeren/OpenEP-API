# Nixpacks Build-Konfiguration für Dokploy
# https://nixpacks.com/docs/configuration/file

[phases.setup]
nixPkgs = ["python312"]

[phases.build]
cmds = ["python -m compileall -q ."]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"

[variables]
PYTHONUNBUFFERED = "1"
PYTHONDONTWRITEBYTECODE = "1"
